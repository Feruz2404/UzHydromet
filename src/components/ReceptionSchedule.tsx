// This component is intentionally neutralized.
// The standalone reception schedule section has been removed from the homepage.
// Reception day/time is now shown only inside each leadership card.
// Keeping this stub avoids accidental imports from breaking the build.

export function ReceptionSchedule() {
  return null
}

export default ReceptionSchedule
