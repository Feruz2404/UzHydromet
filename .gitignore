node_modules
dist

# Local env files — real values must live ONLY in Vercel Environment Variables.
# Never commit real secrets to git.
.env
.env.local
.env.production
.env.development
.env.*.local

.DS_Store
*.log
